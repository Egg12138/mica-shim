package cntr

// TODO: FROM OCI image labels, read <defs.MicaAnnotationPrefix>.client.firmware
func ReadFirmwarePath(bundle string, name string) string {
	
	// prefix := defs.MicaAnnotationPrefix
	// 1. read image labels
	// 2. if found, return the joi(bundle, path)
	// 3. if not found, return ""
	return ""
}